const {filterGoods} = require('./task1');
const {findMostExpensiveGood} = require('./task2');
const {remapGoods} = require('./task3');

module.exports = {
filterGoods,
findMostExpensiveGood,
remapGoods,
}